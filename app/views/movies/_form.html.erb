<%= form_with(model: @movie, local: true) do |form| %>
    <% if @movie.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>
  
        <ul>
        <% @movie.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :movies_name %>
      <%= form.text_field :movies_name %>
    </div>
  
    <div class="field">
      <%= form.label :description %>
      <%= form.text_area :description %>
    </div>
  
    <div class="field">
      <%= form.label :movie_length %>
      <%= form.text_field :movie_length %>
    </div>
 
  <div class="input-field col s6">
        <%= form.label :poster, 'The poster ' %>
        <%= form.url_field :poster %>
    </div>

<div class="input-field col s6">
<%= form.label :tyoler ,'The tyoler ' %>
<%= form.text_field :tyoler %>
</div>

        
    <div class="field">
      <%= form.label :actors_name %>
      <%= form.text_field :actors_name %>
    </div>
  
    <div class="field">
    <%= form.label :rating,'The Rating '%>
    <%= form.text_field :rating%>
  </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  
